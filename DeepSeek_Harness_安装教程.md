# DeepSeek Harness（DSH）安装与使用教程（零基础版）

> 本教程面向**完全没装过 Node.js、没接触过命令行**的朋友。
> 全程以 **Windows 10 / 11** 为例（Mac 的差别很小，见文末附录）。
> 按顺序照做即可，遇到问题直接跳到「第 6 章：常见问题排查」。

---

## 目录

1. [准备工作：先搞懂要装什么](#1-准备工作先搞懂要装什么)
2. [安装 Node.js（必装，一次性）](#2-安装-nodejs必装一次性)
3. [启动 DeepSeek Harness](#3-启动-deepseek-harness)
4. [配置 API 密钥（第一次用必须做）](#4-配置-api-密钥第一次用必须做)
5. [开始使用](#5-开始使用)
6. [常见问题排查（FAQ）](#6-常见问题排查faq)
7. [卸载与更新](#7-卸载与更新)
8. [附录：给 Mac 用户的说明 / 小词典](#8-附录)

---

> 📸 **截图与插图通用说明（写给教程作者：配图完成后，把本说明和各条「截图指引」整段删除，教程即可发布）**
>
> - **截图工具**：Windows 自带「截图工具」（Win 搜索"截图"），或按 `Win + Shift + S` 拖选区域；微信/QQ 的截图、Snipaste 也可以。
> - **标注方法**：截图后在「画图」（Win 搜索"画图"）里按 `Ctrl + V` 粘贴，用「矩形」框和「文字」工具加注释；或直接用微信截图弹出的小工具栏标注。
> - **插入 Word**：把光标放到文中对应「截图指引」的位置 → 菜单「插入」→「图片」→「此设备」→ 选择截图文件。
> - **图片建议**：PNG 格式，宽度 12～15 厘米（插入后选中图片，右上角「布局选项」→「四周型」，或直接拖拽角点缩放）。
> - **命名**：按各条指引建议的文件名（`01-`、`02-`…）命名，方便对号入座。

## 1. 准备工作：先搞懂要装什么

我们要装两样东西：

| 名称 | 是什么 | 用来干嘛 |
|---|---|---|
| **Node.js** | 一个运行 JavaScript 程序的"运行环境" | DeepSeek Harness 本身是 JavaScript 程序，必须靠它才能跑起来。装 Node.js 时会**自动附带** npm（包管理器），后面靠 npm 下载 Harness |
| **DeepSeek Harness（dsh）** | 一个 AI 编程助手（Web 界面版） | 装上后会在你电脑上启动一个本地网页（`http://127.0.0.1:3080`），你在浏览器里跟 AI 对话、让它写代码、改文件 |

另外你还需要：

- **一个 DeepSeek 平台的账号和 API Key**（第 4 章会详细讲，现在不用管）
- 电脑能联网（下载安装包、下载依赖需要）

> 一句话总结流程：**装 Node.js → 用 npm 把 Harness 拉下来 → 启动它 → 填 API Key → 开用**。

---

## 2. 安装 Node.js（必装，一次性）

### 2.1 下载安装包

1. 打开浏览器，访问 Node.js 官网中文站：**https://nodejs.org/zh-cn**
2. 页面上会有两个大按钮：**「LTS」** 和 **「Current」**。
   👉 **一定要点 LTS（长期支持版）**，不要选 Current。LTS 最稳定，适合我们这种用途。
> 📸 **截图指引① ｜ 官网首页（插入位置：2.1 节第 2 步之后）**
> - **拍什么**：浏览器打开 https://nodejs.org/zh-cn 的完整窗口。
> - **标注**：用红框圈出绿色 **LTS** 按钮，旁边加小字「选这个（不要选 Current）」。
> - **命名**：`01-官网首页.png`
> - **插入**：Word 菜单「插入 → 图片 → 此设备」选择该文件，宽度约 14 厘米。
3. 下载下来的文件长这样：`node-v24.x.x-x64.msi`（版本号会随更新变化，只要是 **x64** 就行；32 位老电脑选 x86）。
4. 如果官网打不开或下载很慢，可以用国内镜像下载（内容一样）：
   - 淘宝镜像：`https://npmmirror.com/mirrors/node/`（找 `latest-v24.x/` 文件夹——或者当时最新的 LTS 版本号——下载里面的 `.msi` 文件）

### 2.2 安装（一路"下一步"）

1. 双击刚下载的 `.msi` 安装包。
2. 如果弹出 **用户账户控制（UAC）** 窗口，点 **「是」**。
3. 出现欢迎界面，点 **Next（下一步）**。
4. 勾选 **「I accept the terms...」（我接受条款）**，点 Next。
5. 选择安装路径：**保持默认即可**（一般是 `C:\Program Files\nodejs\`），点 Next。
6. 到了 **Custom Setup（自定义安装）** 页面，**不要改任何选项**，直接点 Next。
   - 注意：这一页最下面的 **"Add to PATH"** 选项必须保持选中（默认就是选中的），它决定了之后能不能在命令行里直接使用 `node` 命令。
7. 点 **Install（安装）**，等进度条走完，点 **Finish（完成）**。

### 2.3 验证是否装成功（重要！）

1. 按键盘 `Win` 键，输入 `cmd`，回车（或者右键开始菜单 →「终端」/「命令提示符」/「PowerShell」）。
2. 在弹出的黑色窗口里输入下面这行，然后回车：

   ```
   node -v
   ```

3. 如果看到类似 `v24.x.x` 的输出（一串以 `v` 开头的版本号），说明 **Node.js 安装成功** ✅
4. 再输入下面这行并回车，验证 npm：

   ```
   npm -v
   ```

   看到一串数字（如 `11.x.x`）就说明 npm 也正常 ✅

5. 如果提示 `'node' 不是内部或外部命令`，说明 PATH 没配上，见 [6.1 节](#61-node-不是内部或外部命令)。

> 装好后**把 cmd 窗口关掉**，后面要用新窗口操作（老窗口可能认不出新装的命令）。

---

## 3. 启动 DeepSeek Harness

打开方式有两种，**新手推荐方法一**（不用安装、命令最简）；如果你打算长期用，再用方法二（装一次，之后命令更短）。

### 方法一：npx 一键运行（推荐先试这个）

1. 打开命令行窗口（`Win` 键输入 `cmd` 回车，**推荐用 cmd**；如果用 PowerShell，注意 6.10 节的执行策略问题）。
2. **先切到一个你想作为"工作目录"的文件夹**（AI 帮你干活时就在这个文件夹里读写文件），例如：
   ```
   cd C:\mywork
   ```
   （`mywork` 可以换成你电脑上的任意文件夹，比如 `cd D:\projects`。第一次用也可以不切，直接用默认目录。）
3. 输入下面这行命令并回车：

   ```
   npx -y @deepseek-ai/dsh web
   ```

4. **第一次运行时会自动下载**（屏幕上会滚动一堆安装信息，像下图这样，耐心等几分钟，取决于网速）：
> 📸 **截图指引② ｜ npx 首次下载过程（插入位置：第 3 章「方法一」第 4 步之后）**
> - **拍什么**：命令行窗口里 `npx -y @deepseek-ai/dsh web` 首次运行时滚动下载的画面（进度条、百分比等）。
> - **提示**：如果下载太快没拍到，可先执行 `npm cache clean --force` 再重跑一次命令，重现下载画面。
> - **命名**：`02-npx下载.png`
5. 下载完会自动启动，**第一次启动会自动初始化配置**（也是自动的，不用管）。
6. 看到类似下面的输出（说明服务已启动）：
   ```
   dsh web: http://127.0.0.1:3080
   ```
> 📸 **截图指引③ ｜ 启动成功画面（插入位置：第 3 章「方法一」第 6 步之后）**
> - **拍什么**：命令行显示服务启动成功、含 `dsh web: http://127.0.0.1:3080` 字样的画面。
> - **标注**：圈出地址行，加小字「在浏览器打开这个地址」。
> - **命名**：`03-启动成功.png`
7. 打开浏览器，在地址栏输入下面的地址，回车，就能看到 DeepSeek Harness 的网页界面了 🎉（如果用 3.1 节的启动脚本启动，脚本会**自动打开浏览器**，可跳过这步）：

   ```
   http://127.0.0.1:3080
   ```

8. **注意：这个命令行窗口不能关**，关了 Harness 就停了。想停掉就按 `Ctrl + C`，或在窗口里输入 `exit`。

> 以后每次启动，重复第 1～3 步即可（第 4 步的下载不会再发生，会直接启动）。

### 方法二：全局安装（适合长期使用）

1. 打开命令行，输入：

   ```
   npm install -g @deepseek-ai/dsh
   ```

2. 等待安装完成（同样会有进度条，可能需要几分钟）。
3. 之后启动只需输入：

   ```
   dsh web
   ```

4. 其余流程（访问 `http://127.0.0.1:3080` 等）和方法一相同。

### 3.1 更省事的启动方式：双击启动脚本

- 首次运行后，通常会在你的用户目录下自动生成一个启动脚本：**`C:\Users\<你的用户名>\.dsh\start-dsh.cmd`**（如果没有，按下面「手动创建」来做）。
- 以后想启动，直接**双击这个文件**就行，它会自动启动服务并打开浏览器；如果 Harness 已经在运行，双击它只会打开浏览器，不会重复启动。
- 你可以右键这个文件 →「发送到」→「桌面快捷方式」，以后从桌面双击即可。
- 想换端口（比如 3080 被占用）：右键「编辑」打开这个文件，把里面**所有**的 `3080` 都替换成新端口（如 `8080`）——文件里一共有 3 处（端口检查、自动打开浏览器、启动命令），都要改。
- **手动创建**（没有自动生成时）：新建一个文本文件，粘贴下面两行，保存后把文件名改为 `start-dsh.cmd`，双击即可运行：

  ```
  @echo off
  npx -y @deepseek-ai/dsh web
  pause
  ```

### 3.2 界面长什么样

> 📸 **截图指引④ ｜ Harness 主界面（插入位置：3.2 节「界面长什么样」开头）**
> - **拍什么**：浏览器里 `http://127.0.0.1:3080` 的完整界面（可先随便发一条消息，让界面饱满一些）。
> - **标注**：圈出并编号：① 左侧会话列表 ② 中间对话区 ③ 底部输入框 ④ 右上角设置（齿轮）。
> - **命名**：`04-主界面.png`

网页打开后你会看到：

- **左侧**：会话列表（可以新建多个会话）
- **中间**：对话区，底部是输入框 —— 在这里跟 AI 说话
- **右上角/齿轮图标**：设置入口（第 4 章要用的 API Key 就在这里填）

---

## 4. 配置 API 密钥（第一次用必须做）

Harness 是调用 DeepSeek 官方大模型的，所以需要一个 **API Key**（相当于"门票+计费凭证"，按用量扣费）。

### 4.1 申请 API Key（去 DeepSeek 开放平台）

1. 打开 **https://platform.deepseek.com** ，用手机号注册/登录。
2. 登录后进入控制台，在左侧菜单找到 **「API Keys」**（API 密钥）页面。
> 📸 **截图指引⑤ ｜ API Keys 页面（插入位置：4.1 节第 2 步之后）**
> - **拍什么**：https://platform.deepseek.com 控制台的「API Keys」页面（登录后从左侧菜单进入）。
> - **标注**：圈出「创建 API Key」按钮。
> - **命名**：`05-APIKeys页面.png`
> - ⚠️ **注意**：创建成功后显示 `sk-` 密钥明文的那一步**不要截图**（密钥等于钱，切勿外发）。
3. 点 **「创建 API Key」**，给密钥起个名字（随便填，如 `my-key`），点创建。
4. 创建后会把密钥显示出来，长这样：**`sk-` 开头的一长串字符**：
   ```
   sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
   ```
   ⚠️ **只显示这一次**，请立刻复制保存好（可以先粘到记事本里）。关掉页面就再也看不到了，只能重新创建。
5. **充值**：这个平台是预付费的。在控制台找到 **「充值」/「费用」** 页面，先充一点钱（比如 10～50 元），模型调用才会成功。不充钱会提示余额不足。
6. 把充完值后平台给的**额度提示**截图存好，方便以后查看余额。

> 提示：密钥等同于钱，**不要发给别人**，不要把密钥提交到 GitHub 等公开地方。

### 4.2 把密钥填进 Harness（三选一）

**方式 A：在网页界面里填（最推荐，最简单）**

1. 打开 Harness 网页（`http://127.0.0.1:3080`）。
2. 点右上角 **「设置」**（齿轮图标）。
3. 找到 **「模型」/「模型设置」** 相关的栏目（DeepSeek 官方接口通常在列表里叫 `deepseek-official`）。
4. 找到 **API Key / 密钥** 输入框，把 `sk-` 开头的密钥粘贴进去，点保存。
5. 回到对话页，直接发一条消息测试。**无需重启**，填完就能用。

**方式 B：手动编辑配置文件（方法 A 不行时用）**

1. 打开文件管理器，地址栏输入 `%USERPROFILE%\.dsh` 回车（Windows 会自动跳转到 `C:\Users\<你的用户名>\.dsh`）。
2. 用记事本打开里面的 **`.credentials.yaml`** 文件（没有就新建一个，名字必须一字不差）。
3. 在里面写入一行（把 `sk-...` 换成你自己的密钥）：
   ```yaml
   DEEPSEEK_API_KEY: sk-你的密钥
   ```
4. 保存文件即可，**无需重启**——下一条对话请求会自动读取新密钥（如果没生效，再重启 Harness 试试）。

**方式 C：设置系统环境变量（进阶，可选）**

1. `Win` 键搜索「环境变量」→「编辑系统环境变量」→「环境变量」。
2. 在「用户变量」里点「新建」：变量名 `DEEPSEEK_API_KEY`，变量值填你的 `sk-` 密钥，确定。
3. 重启 Harness 生效。

> 三种方式任选其一即可，**方式 A 优先**。密钥格式不对或没填时，对话会报错（见 6.5 节）。

---

## 5. 开始使用

1. 打开 `http://127.0.0.1:3080`。
2. 在底部输入框里输入你的第一个指令，比如：
   > 你好，请介绍一下你自己，并告诉我你能做什么。

   回车发送，AI 会开始回复（思考过程会以"思考中…"的形式展示）。
3. 你可以让它干活，比如：
   - 「帮我把这个项目里的 README 写出来」
   - 「检查一下这段代码有没有 bug：……」
   - 「把 `D:\projects\demo` 文件夹里的报错修一下」
4. Harness 会以"智能体"的方式工作：它能看到你启动时所在的工作目录（第 3 章第 2 步 `cd` 到的文件夹），可以读文件、改文件、运行命令。
5. 常用操作：
   - **新建会话**：左侧的「+」按钮
   - **查看设置**：右上角齿轮
   - **换模型**：设置里的「模型」栏目（不同模型价格/能力不同）
   - **停止生成**：回复过程中点「停止」按钮
   - **关掉**：命令行窗口按 `Ctrl + C`（网页也可以直接关，但服务还在后台跑，建议用 Ctrl+C 彻底停掉）

---

## 6. 常见问题排查（FAQ）

### 6.1 `'node' 不是内部或外部命令` / `node` 无法识别

原因：Node.js 没装成功，或安装时 PATH 没勾上。

解决：
1. 确认安装确实完成了（重新跑一遍 2.2 的安装向导，勾选 **Add to PATH**）。
2. **关掉所有命令行窗口再重新打开**，再试 `node -v`。
3. 还不行就手动加 PATH：`Win` 搜「环境变量」→「编辑系统环境变量」→「环境变量」→ 在「用户变量」/「系统变量」的 **Path** 里「新建」，填入 `C:\Program Files\nodejs\`，一路确定，重开命令行。

### 6.2 提示 `npx 不是内部或外部命令`

同上，说明 npm/npx 没装好。重装 Node.js 即可（npx 是 npm 自带的）。

### 6.3 下载很慢 / 卡在下载不动 / 报网络错误（ETIMEDOUT、ECONNRESET）

原因：npm 默认从国外源下载，国内网络不稳定。

解决：把 npm 换成国内镜像（淘宝 npmmirror），在命令行执行：

```
npm config set registry https://registry.npmmirror.com
```

然后重新运行启动命令。想恢复官方源：`npm config set registry https://registry.npmjs.org`。

### 6.4 端口被占用（`EADDRINUSE` / 端口 3080 无法监听）

原因：3080 端口被别的程序占了（或旧实例没退干净）。

解决：换一个端口启动：

```
npx -y @deepseek-ai/dsh web --port 8080
```

（注意 `--port` 要写在 `web` 后面。）然后浏览器访问 `http://127.0.0.1:8080`。改端口后，`start-dsh.cmd` 脚本里的端口也要相应改（见 3.1 节）。

### 6.5 对话报错：`MISSING_CREDENTIAL` / `401` / `AUTH` / `Invalid API key`

原因：API Key 没填、填错了，或密钥已失效。

解决：
- 回到第 4 章，确认密钥已在设置里填好（方式 A），且一字不差（含 `sk-` 前缀）。
- 到 platform.deepseek.com 的 API Keys 页面看看密钥是否还在/被删除。
- 密钥填完记得**保存**；配置文件方式一般**保存即生效**，没生效再重启。

### 6.6 提示 `402` / `Insufficient Balance` / 余额不足 / `QUOTA`

原因：DeepSeek 平台余额不足。

解决：去 https://platform.deepseek.com 充值后再试。

### 6.7 第一次运行时报错一大段红字

- 如果是 **权限不足（EACCES / access denied）**：右键命令行 →「以管理员身份运行」，重试。
- 如果 Windows 弹出 **防火墙提示**：勾选「专用网络」并点「允许访问」，否则浏览器可能连不上本地服务。
- 如果报错信息里有 **`pnpm` 或 `node-gyp`**：一般是网络问题导致依赖没装全，先做 6.3 换镜像，再**删除 `C:\Users\<你的用户名>\.dsh\profiles` 文件夹**，重新运行启动命令让它重新初始化。

### 6.8 浏览器打开是空白页 / 显示"无法访问此网站"

1. 确认命令行窗口还开着、没报错退出。
2. 确认地址是 `http://127.0.0.1:3080`（不是 https，不是 3000）。
3. 换浏览器试试（Chrome / Edge）。
4. 若改了端口，访问对应端口地址（6.4）。

### 6.9 网页能打开但 AI 回复"卡住不动" / 一直"思考中"

- 先等一会儿（思考模式回复慢，长任务几分钟正常）。
- 还不行就检查 6.5（密钥）、6.6（余额）。
- 都不行就 `Ctrl+C` 重启 Harness 再试。

### 6.10 在 PowerShell 里运行 `npm` / `npx` 提示"无法加载 ps1 脚本"

原因：Windows 的 PowerShell 默认禁止运行 `.ps1` 脚本，而 npm/npx 在 PowerShell 里正是通过 `.ps1` 启动的（在 cmd 里则不受影响）。

解决（任选其一）：

1. 改用 **cmd**（`Win` 键输入 `cmd` 回车）来运行 npm/npx 命令，不受此限制——**推荐**。
2. 在 PowerShell 里执行下面这行，允许当前用户运行本地脚本：

   ```
   Set-ExecutionPolicy -Scope CurrentUser RemoteSigned
   ```

   提示确认时输入 `Y` 回车，以后 npm/npx 就能在 PowerShell 里用了。

---

## 7. 卸载与更新

### 更新

- **方法一（npx 方式）**：npx 每次运行都会自动检查/使用最新版，无需手动更新。
- **方法二（全局安装）**：执行
  ```
  npm update -g @deepseek-ai/dsh
  ```

### 卸载

1. 卸载 Node.js：`Win` 搜「控制面板」→「程序和功能」→ 找到 **Node.js** → 右键卸载。
2. 删除 Harness 的数据目录（可选，想彻底删掉就做）：删除 `C:\Users\<你的用户名>\.dsh` 文件夹。
3. （如果是全局安装的）再执行 `npm uninstall -g @deepseek-ai/dsh`。

---

## 8. 附录

### 8.1 给 Mac 用户的说明

- 安装 Node.js：去官网下载 **macOS 的 `.pkg` 安装包**（同样选 LTS），双击安装。
- 打开"终端"（Terminal，`Command + 空格` 搜索），之后的命令全部一样：
  ```
  npx -y @deepseek-ai/dsh web
  ```
- 数据目录在 `~/.dsh`（即 `/Users/<你的用户名>/.dsh`）。
- 其余章节（API Key、FAQ）完全相同。

### 8.2 小词典

| 名词 | 意思 |
|---|---|
| Node.js | 让 JavaScript 程序能在电脑上运行的环境 |
| npm | Node.js 自带的"应用商店/包管理器"，负责下载和安装软件包 |
| npx | npm 附带的小工具，不用先安装就能直接运行某个包（用完留在缓存里） |
| dsh | DeepSeek Harness 的命令行启动器 |
| API Key | 一串 `sk-` 开头的密钥，用于调用 DeepSeek 模型并计费 |
| 端口 | 电脑上区分不同网络服务的编号，Harness 默认用 3080 |
| 127.0.0.1 | 本机地址（"localhost"），代表"就是你这台电脑" |
| LTS | 长期支持版，软件的最稳定版本线 |
| PATH | 系统查找可执行程序的路径列表，`node` 命令能直接用就靠它 |

### 8.3 常用命令速查

```text
node -v                                    # 查看 Node 版本
npm -v                                     # 查看 npm 版本
Set-ExecutionPolicy -Scope CurrentUser RemoteSigned   # PowerShell 用不了 npm/npx 时先执行一次
npm config set registry https://registry.npmmirror.com   # 换国内镜像（下载慢时用）
npx -y @deepseek-ai/dsh web                # 方式一：一键启动（推荐）
dsh web                                    # 方式二：全局安装后启动
dsh web --port 8080                        # 换端口启动
npm install -g @deepseek-ai/dsh            # 全局安装 dsh
npm update -g @deepseek-ai/dsh             # 更新 dsh
Ctrl + C                                   # 在命令行窗口里停止 Harness
```

---

*教程结束。如果按步骤走完还是有问题，把命令行窗口里的报错信息（红字部分）截图发给懂行的朋友，他们一看就能帮你定位。*
